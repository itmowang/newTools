# Toolbox Assistant - 快速开始

## 📥 下载与安装

### 方式 1: 便携版（推荐测试使用）

1. 找到文件：`apps/desktop/release/ToolboxAssistant-Portable.exe`
2. 双击运行
3. 完成！无需安装

### 方式 2: 安装版

1. 找到文件：`apps/desktop/release/Toolbox Assistant Setup 1.0.0.exe`
2. 双击运行安装程序
3. 按照向导完成安装
4. 从桌面或开始菜单启动

## 🚀 首次使用

### 1. 启动应用

双击应用后，你会看到：

```
┌─────────────────────────────────────┐
│ 🧰 Toolbox Assistant      [─] [×]  │ ← 自定义标题栏
├─────────────────────────────────────┤
│                                     │
│          🧰 (浮动动画)               │
│      Toolbox Assistant              │
│   Windows 桌面工具箱                 │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │ 🔌       │  │ 🎨       │        │
│  │端口管理器 │  │ 取色器    │        │
│  └──────────┘  └──────────┘        │
│                                     │
│  快捷键: Ctrl+Shift+P / C          │
└─────────────────────────────────────┘
```

同时：
- 系统托盘出现 🧰 图标
- 右下角出现浮动工具栏

### 2. 使用端口管理器

**启动方式**（任选其一）：
- 点击主窗口的"端口管理器"卡片
- 按快捷键 `Ctrl+Shift+P`
- 右键托盘图标 → 端口管理器
- 点击浮动工具栏 → 端口管理器

**功能说明**：
```
┌─────────────────────────────────────┐
│ 🔌 端口管理器            [─] [×]    │
├─────────────────────────────────────┤
│ 🔌 端口管理器                       │
│ [搜索框...]          [🔄 刷新]     │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │协议│地址│端口│PID│进程│操作│    │ │
│ ├─────────────────────────────────┤ │
│ │TCP │0.0.0.0│80│4│System│❌│    │ │
│ │TCP │0.0.0.0│443│4│System│❌│   │ │
│ │TCP │0.0.0.0│3000│1234│node│❌│ │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 总计: 445 端口 | TCP: 400 | UDP: 45│
└─────────────────────────────────────┘
```

**操作步骤**：
1. 查看端口列表
2. 在搜索框输入端口号或进程名
3. 点击"结束"按钮关闭进程
4. 如需管理员权限，会弹出 UAC 提示
5. 点击"是"授权

### 3. 使用取色器

**启动方式**（任选其一）：
- 点击主窗口的"取色器"卡片
- 按快捷键 `Ctrl+Shift+C`
- 右键托盘图标 → 取色器
- 点击浮动工具栏 → 取色器

**使用步骤**：
1. 启动后全屏显示取色界面
2. 移动鼠标到目标位置
3. 查看底部的颜色预览
4. 点击鼠标选择颜色
5. 颜色自动复制到剪贴板
6. 按 `ESC` 取消

**界面说明**：
```
┌─────────────────────────────────────┐
│ 🎨 取色器已激活                     │
│ 点击选择颜色 • ESC 取消             │
│                                     │
│              ⭕ ← 蓝色光标指示器     │
│                                     │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ [色块] #FF5733                  │ │
│ │        RGB: 255, 87, 51         │ │
│ │        HSL: 12°, 100%, 60%      │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 4. 使用浮动工具栏

**位置**：右下角

**操作**：
- 点击浮动球 🧰 展开工具列表
- 点击工具项启动对应功能
- 拖动可移动位置
- 点击关闭按钮收起

```
收起状态：
  ┌────┐
  │ 🧰 │
  └────┘

展开状态：
  ┌──────────────┐
  │ 🧰 工具箱 [×]│
  ├──────────────┤
  │ 🔌 端口管理器 │
  │ 🎨 取色器     │
  └──────────────┘
```

### 5. 使用系统托盘

**位置**：任务栏右下角

**操作**：
- 右键托盘图标显示菜单
- 选择功能启动
- 选择"退出"关闭应用

```
右键菜单：
  ┌──────────────┐
  │ 打开主窗口    │
  ├──────────────┤
  │ 🔌 端口管理器 │
  │ 🎨 取色器     │
  ├──────────────┤
  │ 退出          │
  └──────────────┘
```

## ⌨️ 快捷键

| 功能 | 快捷键 |
|------|--------|
| 端口管理器 | `Ctrl+Shift+P` |
| 取色器 | `Ctrl+Shift+C` |

## 💡 使用技巧

### 端口管理器

**快速查找端口**：
```
搜索 "80"     → 找到 80 端口
搜索 "node"   → 找到所有 Node.js 进程
搜索 "1234"   → 找到 PID 1234 的进程
```

**结束进程**：
- 普通进程：直接结束
- 系统进程：会弹出 UAC，点击"是"

### 取色器

**取色技巧**：
1. 移动鼠标到目标位置
2. 等待颜色预览更新
3. 确认颜色正确后点击
4. 颜色已在剪贴板，直接粘贴使用

**支持的位置**：
- ✅ 桌面背景
- ✅ 浏览器网页
- ✅ 应用程序窗口
- ✅ 图片查看器
- ✅ 设计软件

### 浮动工具栏

**自定义位置**：
1. 拖动浮动球到喜欢的位置
2. 位置会自动保存（未来版本）

## ⚠️ 常见问题

### Q: 应用无法启动？
A: 
1. 确认是 Windows 10/11 64-bit
2. 右键 → 属性 → 解除锁定
3. 关闭杀毒软件重试

### Q: 端口管理器显示空白？
A: 
1. 点击"刷新"按钮
2. 等待 1-2 秒加载
3. 如仍然空白，重启应用

### Q: 无法结束进程？
A: 
1. 确认点击了 UAC 的"是"
2. 某些系统进程无法结束（正常）
3. 尝试以管理员身份运行应用

### Q: 取色器颜色不准确？
A: 
1. 等待颜色预览更新
2. 确认鼠标在目标位置
3. 多显示器环境下确认在正确的屏幕

### Q: 快捷键不工作？
A: 
1. 确认应用正在运行
2. 检查是否被其他软件占用
3. 重启应用

### Q: 浮动工具栏消失了？
A: 
1. 可能在屏幕边缘
2. 重启应用会重置位置
3. 从托盘重新打开主窗口

## 🔄 退出应用

**方式 1**：右键托盘图标 → 退出

**方式 2**：关闭所有窗口后，右键托盘 → 退出

**注意**：点击主窗口的关闭按钮只会隐藏窗口，不会退出应用

## 📞 需要帮助？

如遇到问题：
1. 查看 `TEST_GUIDE.md` 测试指南
2. 查看 `RELEASE_NOTES.md` 发布说明
3. 提交问题反馈

---

**祝使用愉快！** 🎉
